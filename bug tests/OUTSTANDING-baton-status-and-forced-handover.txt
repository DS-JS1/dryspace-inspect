OUTSTANDING TEST — BATON STATUS AND FORCED HANDOVER
Written 21 August 2026. Not yet done.

Build required: v1.4.0-25 or later. Check the footer of the beta app; it names
the build it is actually running.

Beta app:  https://ds-js1.github.io/dryspace-inspect/beta/
Live app:  https://ds-js1.github.io/dryspace-inspect/   (v1.3.0 — do not test here)


WHY THIS TEST MATTERS
---------------------------------------------------------------------------
Two features have never been exercised end to end against real SharePoint:

  1. The baton status columns — whether the app actually writes them, and
     whether the colours appear as intended.
  2. Forced handover — the administrator recovery path for a lost, broken or
     abandoned device.

Everything else in v1.4 has been tested on a real device. These two have not.


ALREADY CHECKED FOR YOU - 21 August 2026
---------------------------------------------------------------------------
These were verified against the code, so you do not need to worry about them:

  * The beta app IS running the right code. Every app file deployed at /beta/
    is identical to the finished v1.4 build, and it carries v1.4.0-25.
  * The colour formatting JSON in "03_Handover and Version Control Protocol",
    section 10 item 7, DOES include purple for "Recovered".
  * The words in that JSON match the words the app writes exactly - Waiting,
    In progress, Recovered.
  * Step 5's odd-looking expected result, "Jamie Stone, recovered from Jamie
    Stone", is CORRECT. That is genuinely what the app writes.

  STILL YOUR JOB, and it is the important one: check in SharePoint that the two
  columns really are named exactly BatonStatus and BatonHolder. A wrong name
  fails SILENTLY - on purpose, so a column problem can never block a handover.
  It will look like "the app didn't write the status" when the app is fine.
  Check the column names before you start, not after something looks wrong.


BEFORE YOU START
---------------------------------------------------------------------------
1. Delete the test folders from the Site Inspections library.
   KEEP the _diagnostics folder.
   Deleted items sit in the recycle bin for 93 days (tenant default).
   Nothing on a device is affected — that storage is entirely separate.

2. Confirm the two library columns exist, named EXACTLY:
        BatonStatus
        BatonHolder
   The app writes to those names. A mismatch fails silently, by design —
   a column problem must never block a handover.

3. Paste the column formatting into BatonStatus. The JSON is recorded in
   "03_Handover and Version Control Protocol.md", section 10, item 7 -
   copy it from there rather than retyping it:
   Column header > Column settings > Format this column > Advanced mode
   (the link sits at the BOTTOM of that pane, above Save/Cancel).
   Replace everything already in the box.

   The formatting must include PURPLE for "Recovered", or step 5 below will
   show grey and look like a failure when it is not.

4. Open the beta app and confirm the footer reads:  build v1.4.0-25
   If it reads anything lower, the cache has not turned over. Use
   "Check for app update" on the home screen, or load
   .../beta/index.html?nosw=1 once and then the normal address.


THE TEST
---------------------------------------------------------------------------
Step 1 — Create and upload
  New inspection. Fill in client, address, date, inspector name AND the
  inspection number before attaching anything.
  Attach two photos. Upload them.
  EXPECT: both upload; the form's Upload button disappears when done.

Step 2 — Hand over
  Wait a minute or two with the form open, so an automatic wip/ backup is
  written. (Backups run every four minutes and on leaving the form. Step 5
  needs one to exist.)
  Hand over through SharePoint.
  EXPECT in the library:
        BatonStatus = Waiting        shown GREEN
        BatonHolder = your name
  EXPECT on the app home screen:
        the card reads HANDED OVER and sorts below any live records.

Step 3 — Take it back
  Home screen > Take over an inspection from SharePoint.
  EXPECT: the picker lists it with a green WAITING badge at top left.
  Take it over.
  EXPECT in the library:
        BatonStatus = In progress    shown AMBER
        BatonHolder = your name

Step 4 — Browse and view
  Home screen > Browse all inspections.
  EXPECT: it reports the total in the library and how many are waiting.
  Tap the inspection. It should offer:
        Ask for the baton
        Force the handover     <- administrator only; you should see this
        View it
        Open the folder in SharePoint
  Choose View it.
  EXPECT: the form opens with every field greyed out and a black banner
          reading VIEW ONLY — nothing you change here is saved.
  Close it.

Step 5 — Force the handover  ** THE ONE THAT HAS NEVER BEEN RUN **
  Browse all inspections > the same inspection > Force the handover.
  Read the confirmation, then confirm.
  EXPECT in the library:
        BatonStatus = Recovered      shown PURPLE
        BatonHolder = Jamie Stone, recovered from Jamie Stone
  Yes, both names being yours looks absurd. That is correct — you are both
  the administrator and the holder. It proves the whole path.
  EXPECT: the inspection is back on your home screen, with an audit entry
          recording that the handover was forced and where it came from.

  If it refuses with "There is nothing in this folder to recover", there is
  no wip/ backup and nothing in archive/ yet. Hand over a second time to put
  something in archive/, then retry.

Step 6 — Storage
  Check the footer figure for device storage.
  Before this work it read 139.9 MB with no inspections on the device, because
  deleted photos were leaving their bytes behind. That is fixed and a start-up
  sweep reclaims the old ones.
  EXPECT: a small figure that roughly matches what is actually on the device.
  If it is still large with nothing on the home screen, say so — it means the
  sweep is not doing its job.


IF SOMETHING FAILS
---------------------------------------------------------------------------
Do NOT reload the app first. A reload runs start-up recovery and rewrites the
very state worth looking at.

Go straight to:
    https://ds-js1.github.io/dryspace-inspect/beta/diagnostics.html
Tap "Read the stored state", then "Copy everything", and keep it.

That page shows two readings: what was there when the page opened, before the
app booted, and what is there now. The first is the one that matters.

Also note the build from the footer. Several builds all report v1.4.0, so the
build number is the only thing that identifies which code was running.


WHAT IS DELIBERATELY NOT COVERED
---------------------------------------------------------------------------
- A device that dies BETWEEN backups loses up to four minutes of work. No
  feature fixes that; it belongs in the training.
- Photos uploaded to a record that has already been handed over are not
  referenced by the record in SharePoint (D53). The app warns and names the
  remedy — hand over again. The real fix is for a takeover to reconcile
  against the photos/ folder, and it is not built.
- Julie and Mike's permission question: whether Contribute - No Delete permits
  the archive move. Answer it by handing one inspection over TWICE and looking
  at archive/. Open since before this work started.


===========================================================================
RESULTS — RUN 1, 21 August 2026, iPhone (Safari, in browser, build v1.4.0-25)
===========================================================================

Step 1  Create and upload ................................. PASS
Step 2  Hand over — wip/ written, Waiting shown GREEN,
        BatonHolder = Jamie Stone ......................... PASS
Step 3  Take it back — In progress shown AMBER ............ PASS
Step 4  Browse and view — VIEW ONLY banner, thumbnails .... PASS
Step 5  Force the handover ................ NOT REACHED — see below
Step 6  Storage ........................... FAIL / unexplained — see below

ANSWERED: Julie and Mike's permission question. Contribute - No Delete DOES
permit the archive move. archive/ held 11 files after repeated handovers.
That question is closed.


FINDING 1 — "Force the handover" could not be reached, and may be unreachable
---------------------------------------------------------------------------
Observed: the app offered "Take it over", never "Force the handover". The
takeover then said "This inspection already exists on this device" and opened
like a normal takeover. BatonStatus never went purple/Recovered.

This is NOT the app misbehaving at the point of use. Reading the code:

  batonState() (index.html:3659) decides the state from ONE thing - whether a
  file is sitting in current/:
      file in current/            -> 'waiting'   (offers "Take it over")
      no file, no BatonHolder     -> 'none'
      no file, BatonHolder set    -> 'held'      (offers "Ask for the baton"
                                                  + "Force the handover")

  The picker (index.html:3903) offers "Take it over" whenever chosen.file
  exists, and only offers "Ask for the baton" / "Force the handover" in the
  ELSE branch - so both need current/ to be EMPTY.

  But nothing empties current/. takeOverChosen() (index.html:4096) READS the
  file and sets BatonStatus = In progress; it does not move or delete it. The
  handover (index.html:3274) uploads the NEW file into current/ and moves the
  PREVIOUSLY HELD one into archive/. So from the first handover onward,
  current/ always contains exactly one file.

  If that reading is right, 'held' is unreachable through normal use, and so
  are BOTH "Ask for the baton" and "Force the handover". That would explain
  why this test has never been passed: you cannot get to the button.

  Note also that the app and the column DISAGREE by design in this state - the
  column says "In progress" (written at takeover) while the picker computes
  "WAITING" from the file being present. Documented at 03 section 10: the app
  never reads BatonStatus back.

NOT YET PROVEN. The confirming check is in "NEXT RUN" below.


FINDING 2 — Subfolder columns showing grey is CORRECT
---------------------------------------------------------------------------
photos/, wip/, current/ and archive/ show "Not handed over" in grey because
they have no BatonStatus value, and the column formatting renders an empty
value as that text. Only the top-level inspection folder carries the baton.
Read the library at /Documents, not inside a folder. Nothing to fix.


FINDING 3 — Device storage is not being reclaimed
---------------------------------------------------------------------------
  10:02pm  137.5 MB
  10:57pm  147.0 MB     (+9.5 MB across the test)

With ONE inspection on the device, its photos uploaded and verified, and every
media record reporting bytes=MISSING - meaning no local copies are held.

Deleting the folders in SharePoint is irrelevant here and does not reduce
device storage; the two stores are entirely separate.

Unexplained and NOT diagnosed. Candidates, none confirmed:
  - orphaned rows in the 'bytes' store that the start-up sweep is not
    reclaiming (note: the unit test covering exactly this is the one that
    keeps failing - docs/HANDOFF-orphan-bytes-race.md)
  - media records left behind when an inspection is deleted: the store held
    SIX records (IMG_9610/9615/9616/9617/9644/9668) for a device carrying one
    two-photo inspection
  - legacy v1.3 records, or service worker caches. NOTE the storage figure is
    whole-origin: ds-js1.github.io covers the LIVE v1.3 app and the beta
    together, so it is not solely the beta's

GAP IN THE TOOLING: diagnostics.html only ever reads the 'media' store
(line 251, line 486). It cannot see the 'bytes' store or the 'inspections'
store, so it cannot say where the space has gone. Step 6 can raise the alarm
but cannot diagnose it.


NEXT RUN — do these two things
---------------------------------------------------------------------------
A. Confirm or kill Finding 1. In SharePoint, open the inspection folder and
   MOVE the file out of current/ into archive/ by hand. Do not delete it.
   Leave BatonHolder as it is. Then in the app: Browse all inspections > that
   inspection.
     If it now offers "Ask for the baton" and "Force the handover", the
     feature works and the fault is that no normal flow ever reaches it.
     If it still offers only "Take it over", something else is wrong.
   Then run Step 5 from there.

B. Measure Finding 3 decisively. Note the footer figure. Delete the one
   inspection from the DEVICE. Fully close and reopen the app so start-up
   recovery runs. Read the footer figure again.
     If it is still ~147 MB with nothing on the device, the sweep is not
     reclaiming and that is a real fault.

TEST-SCRIPT FAULT, fixed for next time: Step 4 says you "should see" Force the
handover. That is only true when current/ is empty. Do not hand the inspection
over again between Step 3 and Step 5 - it refills current/ and puts you back
to "Take it over".


===========================================================================
RUN 1, PART 2 — the confirming check was done. Finding 1 was WRONG, and the
real fault is worse.
===========================================================================

Check A was run: the file was moved out of current/ into archive/ by hand.

RESULT: the app now offered "Ask for the baton", "View it" and "Open the folder
in SharePoint" - but still NOT "Force the handover". The badge read:

        Baton: NOT HANDED OVER
        "photos uploaded, but the record has never been sent"

while the library, at the same moment, showed that same folder as
BatonStatus = Waiting (green), BatonHolder = Jamie Stone.

So emptying current/ DID change the state - the file half of the logic works,
and the earlier theory that nothing ever empties current/ was wrong. The real
fault is that the app is not receiving the library columns at all:
BatonHolder reads as empty, so batonState() can never return 'held', so
"Force the handover" can never be offered to anyone.

Suspected cause is one line - ds-sharepoint.js:350 uses
expand=listItem($select=fields), which selects the fields property without
expanding it. Full brief, including what must be proven before it is changed:
docs/HANDOFF-baton-columns-not-read-and-storage.md

STORAGE, continued: with ALL inspections deleted from the device and the home
screen reading "No inspections yet", the footer still reported 140.5 MB. The
fault this was supposed to have fixed reported 139.9 MB. Confirmed as a real
anomaly; still not diagnosed, and note the figure is whole-origin so it covers
the live v1.3 app as well as the beta.


================================================================================
RUN 2 - 22 August 2026, real iPhone (iOS 18.7, Safari 26.6), real SharePoint
================================================================================

FAULT 1 IS CONFIRMED AND FIXED. The suspicion from run 1 was proved before
anything was changed, using diagnostics.html section 7, which takes three
readings of the same library root in the same moment.

    Reading 1  the app's own list()          0 of 3 folders carried columns
    Reading 2  the old query, raw            0 of 3
    Reading 3  $expand=listItem($expand=fields)   3 of 3

The raw responses are the argument. Under the old form listItem came back as
{"@odata.etag": ...} and nothing else. Under the documented form the same
folder carried its fields, and Graph's own context URL acknowledged the
expansion as listItem(fields()). The folder read:

    INS - 22xx - Frinight TestClient
    BatonStatus = Waiting   BatonHolder = Jamie Stone   Stage = S01-OFFICE

Set the whole time, and invisible to the app.

Fixed in ds-sharepoint.js, one line, shipped exactly as tested. Section 7 now
keeps the old form as a CONTROL that must keep returning nothing, so the same
fault cannot come back unnoticed.

STEP 5 IS STILL NOT RUN. "Force the handover" is gated on the 'held' state,
which needs BatonHolder, which is why it could never be offered. The transport
is fixed and proved; the BUTTON has still never been seen. That needs a second
person - every run so far has had one person as both administrator and holder,
which is not what the feature is for.

STORAGE IS RESOLVED. Not an app fault, and not a misread figure either.

  - 137.5 MB reported on a device with no inspections; iOS Settings > Safari >
    Advanced > Website Data independently showed github.io at 145 MB, so the
    space was real.
  - Everything enumerable was measured and empty: three stores with nought
    rows, exactly one database on the origin, one cache at 0.2 MB belonging to
    the current build, localStorage negligible.
  - Cause: WebKit keeps IndexedDB in a file that does not shrink when records
    are deleted. Deleting the DATABASE removes the file.
  - Proved: the delete appeared to time out at 1:10am and the space came back
    anyway - 139.0 MB before, 0.9 MB by 1:24am, tester still signed in and
    having deleted nothing through Settings. A deleteDatabase that times out is
    abandoned by the calling page, NOT cancelled by the browser; it completed
    once the tab holding the database closed.
  - Device now reports 1.6 MB.

A SECOND APP FAULT WAS FOUND ON THE WAY. openDB() handled onblocked but never
set onversionchange, so a tab with the app open never released the database and
any delete or upgrade from another context hung silently. DB_VERSION is 2 - the
next schema change would have deadlocked for anyone with two tabs open. Fixed.

AND THE FOOTER WAS FIXED. "Device storage used by this app" was reporting the
whole origin. It now reports what the app actually holds, which is the line
staff read and were right to be alarmed by.

Full brief for the next session:
docs/HANDOFF-22-august-baton-and-storage.md


================================================================================
RUN 3 - 22 August 2026, real iPhone against real SharePoint, build v1.4.0-30
STEP 5 PASSES. THE TEST IS COMPLETE.
================================================================================

"Force the handover" has been run for the first time. The button that had never
been reached was offered, tapped, and did what it says.

SharePoint library afterwards, for INS - 22xx - Frinight TestClient:

    BatonStatus  = Recovered          shown PURPLE
    BatonHolder  = Jamie Stone, recovered from Jamie Stone

Both names being the same is correct and is what the script predicts - one
person is both the administrator and the holder. The record returned to the
home screen.

ONE PERSON WAS ENOUGH, and the two-device plan was not needed. The gate is
isAdmin() && batonState === 'held'. isAdmin() checks the signed-in address
against SP_CONFIG.admins, which holds one entry. 'held' needs current/ empty
with BatonHolder set - and BatonHolder only has to be somebody, which can be
yourself.

ALSO OBSERVED, and both are recorded in docs/OPEN-ITEMS.md:

OI-12 - the takeover writes BatonStatus = "In progress" to the column
(index.html:4175) but does NOT move the file out of current/. So the library and
the app then disagree about the same folder: SharePoint showed SatTest-10 38am
as "In progress" while the app's browse screen showed it as WAITING, because
batonState() reads the file's presence rather than the column. Both were on
screen at the same moment. How a folder reaches 'held' by ordinary use is still
unanswered - the one used here got there in run 1 by a file being moved out of
current/ by hand.

OI-13 - the handover confirmation is a native alert() and iOS TRUNCATES it. The
message ends mid-sentence at "Delete your copy from", losing "...this device once
they confirm they have it" - the instruction that stops two people holding one
record. Not visible on a desktop browser.

OI-14 - the audit trail records the forced handover but nothing in the app
displays it. The status bar shows a count only; the entries are readable only by
exporting the record as JSON.

WHAT THE BROWSE SCREEN LOOKED LIKE - all three baton states at once, columns
read correctly, which is fault 1's fix working in ordinary use:

    IN PROGRESS      amber  INS - 22xx - Frinight TestClient  held by Jamie Stone
    WAITING          green  SatTest-10 38am - SatTest Client  last held by Jamie Stone
    NOT HANDED OVER  grey   Unfiled                           never sent

Step 6, storage: resolved separately and not by the sweep - see
docs/HANDOFF-22-august-baton-and-storage.md section 4.
